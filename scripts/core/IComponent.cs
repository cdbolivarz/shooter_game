namespace ShooterGame.Core;

/// <summary>
/// Marker contract for all data components attached to entities.
/// Components are plain data (state) with no behavior — logic lives in systems.
/// </summary>
public interface IComponent
{
}
